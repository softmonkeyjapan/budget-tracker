# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `softmonkeyjapan/budget-tracker` (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Default 1:1 mapping (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
